export class Acmer {
  lastName: string;
  handle: string;
  email: string;
  firstName: string;
  country: string;
  rank: string;
  maxRank: string;
  rating: number;
  maxRating: number;
  solvedProblems: number;
  score:number;
  password: string;
  token: string;
  solvedProblemsDetails: string;
  role:Role;


 /* constructor(handle: string, email: string, firstName: string, lastName: string, country: string, rank: string, maxRank: string, rating: number, maxRating: number, problemSolved: number, role: Role) {
    this.lastName = lastName;
    this.handle = handle;
    this.email = email;
    this.firstName = firstName;
    this.country = country;
    this.rank = rank;
    this.maxRank = maxRank;
    this.rating = rating;
    this.maxRating = maxRating;
    this.problemSolved = problemSolved;
    this.role = role;
  };*/


}
